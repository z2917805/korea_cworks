#include <stdio.h>  //C언어에서 제공된 헤더파일
#include "Calculator.h";  //내가 만든 헤더파일

int main_CalculatorMain()
{
	int x = 3, y = 4, result;
	count++;

	result = addition(x, y);

	printf("count = %d\n", count);
	printf("result = %d\n", result);

	return 0;
}
